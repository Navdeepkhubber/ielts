# Production runtime dependencies only.

Flask==3.0.3
PyMuPDF==1.24.9
gunicorn==22.0.0
firebase-admin==6.5.0

# S3-compatible client for fetching test content (PDFs, audio) from
# remote blob storage at request time -- see lib/blob_storage.py.
# Lightweight (pure Python + botocore), unlike the OCR stack above.
boto3==1.34.144
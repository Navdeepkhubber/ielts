Flask==3.0.3
PyMuPDF==1.24.9

# Production WSGI server -- the Flask dev server used by `python3 app.py`
# is single-threaded and explicitly unsafe for real traffic.
gunicorn==22.0.0

# Verifies Firebase ID tokens server-side (login/signup) and provides the
# Firestore client used for user profiles and attempt/progress history --
# see lib/firebase_admin_setup.py, lib/auth.py, lib/storage.py, and
# SETUP.md for the one-time Firebase project setup this needs.
firebase-admin==6.5.0

# S3-compatible client for optional remote test-content storage (B2, R2,
# Wasabi, Spaces, ...) -- see lib/blob_storage.py and SETUP.md. Entirely
# inert if the BLOB_* env vars aren't set; scripts/sync_tests_to_blob_storage.py
# is the only thing that writes to it.
boto3==1.34.144

# Loads a local .env file if present (see .env.example) -- purely a local
# dev convenience, never needed in production. Safe to skip installing;
# app.py's import is wrapped in try/except ImportError.
python-dotenv==1.0.1
